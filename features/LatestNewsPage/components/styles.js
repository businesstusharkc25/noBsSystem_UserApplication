import { StyleSheet } from "react-native";
import { colors, typography } from "../../../styles";

export const styles = StyleSheet.create({
  latestNewsPageContainer: {
    backgroundColor: colors.appBaseBlack.backgroundColor,
  },
});
