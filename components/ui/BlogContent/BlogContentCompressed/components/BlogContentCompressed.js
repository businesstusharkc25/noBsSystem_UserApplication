import React from "react";
import { ContentShortDescription } from "../../../../sharedComponents";

const BlogContentCompressed = ({ contentData }) => {
  return <ContentShortDescription contentData={contentData} />;
};

export default BlogContentCompressed;
