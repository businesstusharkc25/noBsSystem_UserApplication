import React from "react";
import { ChannelInfoVideoPage } from "../../features/ChannelInfoVideoPage";

const ChannelInfoVideos = ({ navigation }) => {
  return <ChannelInfoVideoPage navigation={navigation} />;
};

export default ChannelInfoVideos;
