const appBaseSlug = "/app";

const appApiRoutes = {
  appBaseSlug: appBaseSlug,
  appConnectWalletRoute: `${appBaseSlug}/connect-wallet`,
  getContentRoute: `${appBaseSlug}/get-content`,
  addCommentRoute: `${appBaseSlug}/add-comment`,
};

export default appApiRoutes;
